server:
  port: 8086

spring:
  application:
    name: coupon-service
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
